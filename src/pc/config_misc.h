#pragma once

#define RemasteredLogo 1 // Makes the Logo stay instead of zooming out when the fade comes.
//#define SettingsPlus 1 // Not Sure what to do with it, but it makes some of the settings i made show up. For example, if you enable Real Time Clock Feature, Only on PCs, then it will behave like a RTC.
#define RTC 1
